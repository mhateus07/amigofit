{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
    // O TypeScript 6 não inclui mais @types/* automaticamente.
    "types": ["jest", "node"]
  },
  "exclude": ["node_modules", "server", "ios", "android"]
}
